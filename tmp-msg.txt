ux: remove the section links from the header

Leo asked for these out. They shipped an hour ago in #95 and, seen on the live
site, they crowd the header — eight links between the Home breadcrumb and the
search button.

Removed rather than hidden, so nothing is left computing a list nobody renders:

  NotionPageHeader           the nav, the sections prop, and useRouter, which
                             existed only to mark the active section
  NotionPage                 the prop, its destructure and the pass-through
  resolveNotionPageLocal     stops calling getTopLevelSections on every page
  notion-local               getTopLevelSections and SiteSection, now unused
  notion.css                 the Section navigation block, including the mobile
                             rule that hid breadcrumbs to make room for it — so
                             the full breadcrumb trail comes back on a phone

The 404 page keeps its own section links. It builds them straight from the
manifest rather than through this helper, so it is untouched.

Verified on a rendered production build: zero section links, breadcrumbs, skip
link, search trigger and header all still present. CSS checks clean — 222/222
braces, 56/56 comment delimiters, no orphaned comment bodies. tsc clean, build
clean, 11/11 tests.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_016BfsUTpUTYNtYW9zBYaJu5
